"""Axis pipelines and work-package runners.

Each submodule contains a ``cli.py`` with a ``run(argv)`` function that
parses axis/WP-specific arguments and emits its tidy CSV + provenance
block under the ``--out`` directory.
"""
